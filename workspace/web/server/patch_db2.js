var fs = require('fs');var s = fs.readFileSync('./src/models/database.ts', 'utf8');var start = s.indexOf('async function sqliteRun');var end = s.indexOf('sqliteSave();\n}\n\nasync function sqliteGet', start);if (start > -1 && end > -1) {var replacement = 'function sqliteRun(sql: string, params: any[] = []) {\n  if (!sqlDb) throw new Error('SQLite not initialized');\n  const stmt = sqlDb.prepare(sql);\n  stmt.bind(params);\n  while (stmt.step()) {}\n  stmt.free();\n}';s = s.substring(0, start) + replacement + s.substring(end + 14);} else {console.log('not found:', start, end);}fs.writeFileSync('./src/models/database.ts', s);console.log('patched sqliteRun');
