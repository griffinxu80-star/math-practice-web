var fs = require('fs');\nvar src = fs.readFileSync('./src/models/database.ts', 'utf8');\n\n// Replace async sqliteRun with sync\nsrc = src.replace(/async function sqliteRun\(sql: string, params: any\\[\\] = \\[\\]\\) \\{[\s\S]*?sqliteSave\(\);\n\}/, 'function sqliteRun(sql: string, params: any[] = []) {\\n  if (!sqlDb) throw new Error('SQLite not initialized');\\n  const stmt = sqlDb.prepare(sql);\\n  stmt.bind(params);\\n  while (stmt.step()) {}\\n  stmt.free();\\n}');\n\n// Replace async sqliteGet with sync  \nsrc = src.replace(/async function sqliteGet\(sql: string, params: any\\[\\] = \\[\\]\\) \\{[\s\S]*?return row;\n\}/, 'function sqliteGet(sql: string, params: any[] = []) {\\n  if (!sqlDb) throw new Error('SQLite not initialized');\\n  const stmt = sqlDb.prepare(sql);\\n  stmt.bind(params);\\n  const row = stmt.step() ? stmt.getAsObject() : null;\\n  stmt.free();\\n  return row;\\n}');\n\n// Replace async sqliteAll with sync\nsrc = src.replace(/async function sqliteAll\(sql: string, params: any\\[\\] = \\[\\]\\) \\{[\s\S]*?return rows;\n\}/, 'function sqliteAll(sql: string, params: any[] = []) {\\n  if (!sqlDb) throw new Error('SQLite not initialized');\\n  const stmt = sqlDb.prepare(sql);\\n  stmt.bind(params);\\n  const rows: any[] = [];\\n  while (stmt.step()) {\\n    rows.push(stmt.getAsObject());\\n  }\\n  stmt.free();\\n  return rows;\\n}');\n\n// Replace async sqliteExec with sync\nsrc = src.replace(/async function sqliteExec\(sql: string\\) \\{[\s\S]*?sqliteSave\(\);\n\}/, 'function sqliteExec(sql: string) {\\n  if (!sqlDb) throw new Error('SQLite not initialized');\\n  sqlDb.exec(sql);\\n}');\n\nfs.writeFileSync('./src/models/database.ts', src);\nconsole.log('Patched database.ts');\n
